# Build outputs
build/
apps/android/**/build/
apps/android/.gradle/
*.apk
*.aab
*.so

# Android / Gradle local
apps/android/local.properties
.gradle/

# Go
dist/
*.test
/vendor/

# Secrets — never commit
*.jks
*.keystore
*.p8
*.pem
.env
**/.env.*
!**/.env.example
service-account*.json
*service_account*.json
upload-keystore*

# Editor / OS
.DS_Store
.idea/
*.iml
.vscode/
*~

# Reference evidence must never ship in release packages (P00 gate)
# (packaging scripts must explicitly exclude docs/reference/)
dist-checksums.txt
